---
slug: 39
title: 仮想マシンで固定 IP を設定する（CentOS を例に）
date: '2022-04-22T18:13:42+08:00'
author: hiyoung
# layout: post
# permalink: /archives/39
views:
    - '299'
categories:
    - 技術学習
tags:
    - CentOS
---

{{< notice >}} この記事は gemini-3-flash-preview によって翻訳されました {{< /notice >}}

> この記事は [Hiyoung](https://blog.hiyoung.xyz/) が書いたものだよ。
>
> 元の記事: <https://blog.hiyoung.xyz/2022/09/03/3256bd2afccc/>

## 一、仮想マシンの設定

メニューバーから「編集」→「仮想ネットワークエディター」を選んでダイアログを開く。NAT 接続を選択して、サブネット IP を適当に設定したら、「NAT 設定」をクリックしてサブネットマスクとゲートウェイを確認しよう。これは後で固定 IP を設定する時に使うからね。

VMnet8 モードで NAT 接続を選んで、DHCP サービスのチェックを外す。

「NAT 設定」をクリックして、その時のゲートウェイをメモしておく。

システムのネットワーク設定で、VMnet8 のネットワーク構成を確認してね。

## 二、Linux ホストの設定

root 権限で **cd /etc/sysconfig/network-scripts** と入力してフォルダに移動。**ls** コマンドでフォルダ内のファイルを確認して、下の画像のように `ifcfg-en***` というファイルを探そう。

![image](https://cdn.jsdelivr.net/gh/hiyoung3937/img_hiyoung@master/bolg/%E8%AE%BE%E7%BD%AE%E9%9D%99%E6%80%81IP_5.1dtjdfu2j734.jpg)

`vim ifcfg-en***` でファイルを開く。

`BOOTPROTO="dhcp"` を `BOOTPROTO="static"` に、`ONBOOT="no"` を `ONBOOT="yes"` に書き換える。

そして、その下に以下の項目を追加してね。

IPADDR=*your ip*（固定 IP アドレス）

NETMASK=*your netmask*（サブネットマスク、普通は 255.255.255.0）

GATEWAY=*your gateway*（ゲートウェイアドレス）

DNS1=8.8.8.8（国内外のパブリック DNS で大丈夫）

DNS2=8.8.4.4

最後に `:wq` と入力して保存して終了すれば OK！
